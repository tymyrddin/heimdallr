# heimdallr scenario: routing-hijack
#
# The first routing scenario: inter-domain-simlab's false-origin /25 hijack.
# Loaded with `./ctl up --scenario routing-hijack` or `./ctl scenario routing-hijack`.

name: routing-hijack
substrate: routing
baseline: routing-hijack          # rules/baseline/<this> seeds the workspace

# Inputs to stage into ingest/. Pending inter-domain-simlab exporting them
# (its PLAN sections 8, 14, 15: MRT seed plus a scorer timeline).
inputs:
  - source: inter-domain-simlab
    artefacts:
      - mrt-seed.mrt              # pending export
      - timeline.json            # pending export
    status: pending

brief: brief.md
doctrine: blue/source/docs/counter/network/correlation.md
