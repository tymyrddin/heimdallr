# heimdallr scenario: ot-modbus
#
# An OT scenario over a Modbus capture plus host-event logs from ics-access-simlab.
# Loaded with `./ctl up --scenario ot-modbus` or `./ctl scenario ot-modbus`.

name: ot-modbus
substrate: ot
baseline: ot-modbus

# Pending the ics-access-simlab export adaptation (a pcap-capture wrapper in its
# ctl, plus structured host logging in the Modbus/OT servers). See M3.
inputs:
  - source: ics-access-simlab
    artefacts:
      - modbus.pcap              # pending export
      - host-events.json         # pending export
    status: pending

brief: brief.md
doctrine: blue/source/docs/counter/network/correlation.md
