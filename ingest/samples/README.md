# ingest/samples

Small, committed sample captures, for proving the range boots and for quick
tries. The M1 smoke test wants nothing more than a trivial benign pcap dropped
here (a few captured pings will do).

Keep these tiny. Real, scenario-grade captures stay out of git and land in the
parent `ingest/` directory at run time.
